import {expect} from "chai";

describe("PostAppService", () => {
    it("todo", () => {
        // given

        // when
        const foo = "1";
        // then
        expect(foo).to.eq("1");
    });
})
