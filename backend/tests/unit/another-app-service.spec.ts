import {expect} from "chai";

describe("AnotherAppService", () => {
    it("todo", () => {
        // given

        // when
        const foo = 1;
        // then
        expect(foo).to.eq(1);
    });
})
